## 背景

管理员在查看用户详情时，除了资料和预约历史，还需要快速知道该用户累计预约了多少次、累计签到过多少次。

## 目标

- 在用户详情右侧资料区增加两个累计统计
- 不影响现有预约历史分页和用户列表性能

## 方案

- 打开用户详情弹窗时，按需查询该用户：
  - `reservations` 总数
  - `checkins` 总数
- 使用 `count` 查询，不拉取明细数据
- 结果展示在右侧资料字段中：
  - `预约次数`
  - `签到次数`

## 验证

- 打开用户详情后能看到两个累计次数
- 切换用户时统计跟随刷新
- 关闭弹窗后状态重置
